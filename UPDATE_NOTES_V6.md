# BPLab Trace V6.0 更新说明

## 2026-08-03 受控资料编号修正

- 以最新 `sop-实验原始记录` 目录为唯一依据。
- 翘曲变形试验纠正为 `SOP-006`。
- 热膨胀系数试验确认为 `SOP-007`。
- 弯曲性能试验确认为 `SOP-010`。

- 角色收敛为管理员、样品管理员、实验员、复核员、质量检测员。
- 管理员不再建立委托；样品管理员负责委托、收样和任务下发。
- 任务按样品组自动形成，样品管理员只选择实验员。
- 复核员与质量检测员由系统按独立性和工作量自动匹配。
- 复核通过后系统自动生成报告初稿；质量检测员审核，管理员最终签发。
- 增加平板现场相机、强制拍照节点、服务器时间戳、原图/水印双份留档。
- 取消图片和截图文件上传，仅保留设备原始数据文件入口。
- 修改日志改为只追加、哈希链审计，并保留字段修改前值和修改后值。
- 最终报告生成后旧业务版本作废，普通界面只提供最终有效版本；
  审计区仍保留完整只读快照。
- 增加报告发放登记表。
- 增加客户异议两路径、报告有效性控制和留样重测任务。
- 内部追溯工作簿增加全过程时间轴、现场照片、全量参数和原始数据。
- SOP、原始记录和实验拍照节点更新为最新 `sop-实验原始记录` 内容。
